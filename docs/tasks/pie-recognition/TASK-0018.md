# TASK-0018: パフォーマンス最適化

| 項目 | 値 |
|------|-----|
| 要件名 | pie-recognition |
| タイプ | TDD |
| フェーズ | Phase 4 - 統合・品質 |
| 前提タスク | TASK-0017 |
| 後続タスク | なし |
| 関連要件 | NFR-001（3秒以内）, NFR-002（8-9枚同時認識） |
| 信頼性 | 🟡 NFR要件から妥当な推測 |

## コンテキスト

- SvelteKit 2.50.2 + Svelte 5.51.0, TypeScript 5.9.3, Vitest 4.0.18 + Playwright
- adapter-static（SPA、バックエンドなし）
- SvelteKitルーティング: / (top), /camera (カメラフロー), /result (認識結果), /rules (ルール一覧)
- 認識パイプライン: OpenCV.js検出 → pHash識別 → 加点役判定 → ジャラ計算
- ユビキタス言語: パイ=牌, 加点役=役, ジャラ=点数
- プロジェクトルート: /workspaces/nanjara/develop

## 概要

認識パイプライン全体のパフォーマンスを計測・最適化し、3秒以内の目標を達成する。

## 実装詳細

- パフォーマンス計測の実装 🟡
  - 認識パイプライン各ステップの処理時間計測
  - OpenCV.js検出時間、pHash計算時間、マッチング時間の個別計測
- ボトルネック特定と最適化 🟡
  - 画像縮小サイズの最適化（精度とパフォーマンスのバランス）
  - 2値化閾値パラメータの調整
  - 必要に応じてWeb Worker化を検討
- 8-9枚のパイを含むテスト画像での実機テスト 🟡

## 完了条件

- [ ] 認識パイプラインの各ステップの処理時間が計測できる
- [ ] 8-9枚のパイ画像で3秒以内に認識が完了する（目標）
- [ ] パフォーマンスの計測結果がログに記録される
- [ ] テストが通る

## テスト要件

- パフォーマンス計測が正しく動作することのテスト 🟡
- 処理時間のベンチマークテスト 🟡

## 注意事項

- 実機テスト（スマホ実機）はこのタスクの範囲外（手動で別途実施）
- まずは動くもの優先、精度は後から改善（要件定義方針）
- 3秒以内が達成困難な場合は、ボトルネックを特定して報告
