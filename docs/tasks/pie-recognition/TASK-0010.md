# TASK-0010: カメラ関連ファイルを/cameraルートへ移動

| 項目 | 値 |
|------|-----|
| 要件名 | pie-recognition |
| タイプ | DIRECT |
| フェーズ | Phase 3 - スコアリング・UI |
| 前提タスク | なし（独立して実行可能） |
| 後続タスク | TASK-0011 |
| 関連要件 | REQ-202 |
| 信頼性 | 🔵 SvelteKitルーティング設計（ヒアリングで確定）より |

## コンテキスト

- SvelteKit 2.50.2 + Svelte 5.51.0, TypeScript 5.9.3, Vitest 4.0.18 + Playwright
- adapter-static（SPA、バックエンドなし）
- SvelteKitルーティング: / (top), /camera (カメラフロー), /result (認識結果), /rules (ルール一覧)
- 既存カメラコンポーネント: Camera.svelte, CameraCapture.svelte, CameraResult.svelte, CameraLayout.svelte（すべて app/src/lib/components/camera/ にある）
- 既存ルート: app/src/routes/+page.svelte（現在カメラが直接配置されている）
- ユビキタス言語: パイ=牌, 加点役=役, ジャラ=点数
- プロジェクトルート: /workspaces/nanjara/develop

## 概要

現在 app/src/routes/+page.svelte に直接配置されているカメラ機能を /camera ルートに移動する。ファイル移動のみで、ロジック変更は行わない。gitログで追跡しやすいように移動コミットを分離する。

## 実装詳細

- app/src/routes/camera/+page.svelte を作成（現在の+page.svelteの内容をコピー）🔵
- 現在の app/src/routes/+page.svelte を空にする（TASK-0012でtopページとして実装）🔵
- ファイル移動のみ、import パスやロジックの変更は次のタスク 🔵

## 完了条件

- [ ] app/src/routes/camera/+page.svelte にカメラ機能が配置されている
- [ ] app/src/routes/+page.svelte が空（またはプレースホルダー）
- [ ] ビルドが通る

## 注意事項

- ロジック変更は一切しない（移動のみのコミット）
- git mv を使用してファイル追跡を維持
