# TASK-0006: pHash計算モジュール実装

| 項目 | 値 |
|------|-----|
| 要件名 | pie-recognition |
| タイプ | TDD |
| フェーズ | Phase 2 - 認識エンジン |
| 前提タスク | TASK-0003 |
| 後続タスク | TASK-0008 |
| 関連要件 | REQ-002, REQ-301 |
| 信頼性 | 🔵 tools/hasher/src/phash.ts の既存実装を移植 |

## コンテキスト

- SvelteKit 2.50.2 + Svelte 5.51.0, TypeScript 5.9.3, Vitest 4.0.18 + Playwright
- PaiHashMap = Record<PaiId, string>
- tools/hasher/src/phash.ts にDCTベースpHash既存実装

## 概要

app/src/lib/services/phash.ts を実装。ブラウザ用pHash計算。tools/hasher/src/phash.tsのDCTベースpHashアルゴリズムを移植（sharpの代わりにCanvas APIを使用）。

## 実装詳細

- computePhash(imageData: ImageData): string 🔵
- hammingDistance(hash1: string, hash2: string): number 🔵
- findClosestMatch(hash: string, hashMap: PaiHashMap): {paiId, distance} 🔵
- DCTベース（32x32リサイズ→DCT→8x8低周波→中央値比較→64bitハッシュ）🔵

## テスト

- 同一画像の一致テスト 🔵
- 異なる画像の相違テスト 🔵
- ハミング距離計算テスト 🔵
- findClosestMatchテスト 🔵

## 完了条件

- [ ] phash.ts がexportされている
- [ ] tools/hasher との互換性が確認されている
- [ ] テストが通る
